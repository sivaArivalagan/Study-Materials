DECLARE
CURSOR C1(dno emp.deptno%type) IS SELECT * FROM EMP where deptno=dno;
A EMP%ROWTYPE;
BEGIN
OPEN C1(10);
LOOP
FETCH C1 INTO A;
DBMS_OUTPUT.PUT_LINE (A.EMPNO||'  ' || A.ENAME ||'   '|| A.JOB);
EXIT WHEN C1 % NOTFOUND;
END LOOP;
CLOSE C1;
END;
/
